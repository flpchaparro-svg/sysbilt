/**
 * Quote Capture product pay is no longer a static Payment Link.
 * /go/quote-capture builds a Stripe Checkout Session from the add-on
 * checkboxes (base $2,800, Concierge +$600, quote/invoice setup +$100).
 * After pay → https://sysbilt.com/go/thanks?p=quote-capture
 */
export const QUOTE_CAPTURE_STRIPE_URL = ''
