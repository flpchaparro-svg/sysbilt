/** Dispatched on `window` when Sybil chat open state changes (see `SybilChat`). */
export const SYBIL_CHAT_OPEN_CHANGE_EVENT = 'sybil-chat-open-change';

/** Dispatched on `window` to open Sybil from an external launcher (see `HelpDock`). */
export const OPEN_SYBIL_CHAT_EVENT = 'open-sybil-chat';
